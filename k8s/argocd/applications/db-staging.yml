apiVersion: argoproj.io/v1alpha1
kind: Application

metadata:
  name: mongodb

spec:
  project: staging

  source:
    repoURL: https://github.com/rajarshigit2441139/full-stack_chatApp.git
    targetRevision: k8s
    path: k8s/deploy/mongodb/overlays/staging

  destination:
    server: https://kubernetes.default.svc
    namespace: db

  syncPolicy:
    automated:
      prune: true
      selfHeal: true