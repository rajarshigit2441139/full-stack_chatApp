apiVersion: v1
kind: Service
metadata:
  name: backend-preview-svc
  labels:
    app: backend
    stage: staging
spec:
  selector:
    app: backend

  ports:
    - name: http
      protocol: TCP
      port: 5001
      targetPort: http

  type: ClusterIP