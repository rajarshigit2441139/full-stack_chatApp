apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: monitoring

resources:
  - alertmanager-config.yaml
  - gmail-external-secret.yaml

generatorOptions:
  disableNameSuffixHash: true